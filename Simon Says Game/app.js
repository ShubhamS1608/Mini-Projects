let gameSeq =[];
let userSeq =[];

let started = false;
let level = 0;

document.addEventListener("keypress",function(){
    console.log("Game is started");
    if(started==false);
    level++;
    started==true
    levelUp();
})